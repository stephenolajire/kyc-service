Django==6.0.2
djangorestframework==3.16.1
gunicorn==26.0.0
python-decouple==3.8
python-dotenv==1.2.2
RapidFuzz==3.14.5
requests==2.32.5
numpy==2.4.4
opencv-python-headless==4.13.0.92
pillow==12.1.1

# Heavy ML dependencies. Keep these explicit so Render does not install
# duplicate GUI OpenCV/Flask packages from a frozen local environment.
deepface==0.0.99
tensorflow==2.21.0
tf_keras==2.21.0
easyocr==1.7.2

# Prefer CPU-only PyTorch wheels for EasyOCR on non-GPU hosts.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
torchvision==0.26.0+cpu
